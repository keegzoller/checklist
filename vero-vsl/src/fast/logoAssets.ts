/**
 * OPTIONAL REAL LOGO FILES
 * ------------------------
 * Every brand mark in the video is drawn in code so the render works offline
 * and carries no downloaded assets. If you would rather use an official file,
 * drop it in `public/logos/` and name it here. Anything listed wins over the
 * built-in glyph; anything absent falls back to it. No other code changes.
 *
 *   'vero'  -> replaces the vector re-creation of the Vero droplet, which is
 *              the one worth doing first: the built-in is traced from a
 *              ~180px crop of the screen recording.
 *
 * PNG (transparent, 512px+) or SVG both work.
 */
export const LOGO_FILES: Record<string, string | undefined> = {
  // vero: 'vero.png',
  // toast: 'toast.svg',
  // lightspeed: 'lightspeed.svg',
  // aloha: 'ncr-aloha.svg',
  // simplifi: 'simplifi.svg',
  // klaviyo: 'klaviyo.svg',
};
